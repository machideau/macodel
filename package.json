{
  "name": "ecovalo-website",
  "version": "1.0.0",
  "description": "Site web moderne et interactif pour EcoValo - Gestion des déchets",
  "main": "index.html",
  "scripts": {
    "dev": "browser-sync start --server --files \"*.html, css/*.css, js/*.js\" --port 3000 --no-open"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2"
  }
}
